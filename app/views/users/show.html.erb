<p id="notice"><%= notice %></p>

<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<p>
  <strong>Proposals:</strong>
  <% @user.proposals.each do |p| %>
  <li>Title: <%= link_to p.title, p %> | Job: <%= link_to p.job.title, p.job %> | <%= link_to 'Message', new_proposal_message_path(p) %></li>
  <% end %>
</p>

<%= link_to 'Inbox', inbox_path %> |
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
